# Visual theme. Colours mirror the tokens in ui.py, which styles the custom tiles and charts.
[theme]
base = "dark"
primaryColor = "#3987e5"
backgroundColor = "#0d0d0d"
secondaryBackgroundColor = "#1a1a19"
textColor = "#ffffff"
font = "sans-serif"

[browser]
gatherUsageStats = false
